# Platform genelinde docker-compose tarafından kullanılan temel ortam değişkenleri.

# PostgreSQL Veritabanı Ayarları
POSTGRES_DB=azuraforge

# Paylaşımlı Dizinler (Gerekirse değiştirilebilir)
REPORTS_DIR=./reports
CACHE_DIR=./.cache

# Worker'ın kullanacağı cihaz (cpu veya gpu)
AZURAFORGE_DEVICE=cpu

# --- DİKKAT ---
# API ve Dashboard'a özel URL'ler artık kendi .env dosyalarında yönetilmektedir.